public class create_account{
    public create_account(username,password) {
        print("got the username and passcode")


        //retive userlist
        //place holder for now
        int userlist = new int[100];
        length = userlist.length
        i = 0;
        while (length < i) {
            if (userlist[i] == username or userlost[ i] ==password)
            {
                print("dupicilate need a new username or passcode");
                text = "need new username or passcode";
                return text;


            }
            i += 1;


        }
    //now save using the function for userdatabase
    }

    print("username and passcode sucessfully saved");
    }




